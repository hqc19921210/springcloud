server:
  port: 8081
spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
##开启feign对sentinel fallback 的支持
feign:
  sentinel:
    enabled: true
